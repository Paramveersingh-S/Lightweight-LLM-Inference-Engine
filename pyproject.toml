[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lite_llama"
version = "0.1.0"
description = "Lightweight LLaMA inference engine"
readme = "README.md"
requires-python = ">=3.10"
